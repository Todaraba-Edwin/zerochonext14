import { LayoutProps } from '@/type/layout'
import React from 'react'

export default function AfterLoginLayout({children}:LayoutProps) {
  return (
    <div>AfterLoginLayout레이아웃{children}</div>
  )
}
